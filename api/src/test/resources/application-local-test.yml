spring:
    datasource:
        url: jdbc:tc:mysql:8.4://api?useSSL=false&serverTimezone=Asia/Seoul
        username: root
        password: password
        driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
        properties:
            hibernate:
                format_sql: true
